Use ```npm install``` in this directory to install dependencies.

Start source code watching and automatic compiling to JavaScript using:
```grunt demon```

Tests are run using jasmine-node: ```jasmine-node --coffee tests/```
